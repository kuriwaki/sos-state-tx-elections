% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy_ted_returns}
\alias{tidy_ted_returns}
\title{Reshape raw TED returns into a tidy long table}
\usage{
tidy_ted_returns(raw)
}
\arguments{
\item{raw}{Wide returns as returned by \code{\link[=fetch_ted_returns]{fetch_ted_returns()}}.}
}
\value{
A tibble with one row per precinct-candidate and columns \code{cntyvtd},
\code{county_fips}, \code{candidate}, \code{party}, \code{year}, \code{election_type}, \code{office},
and \code{votes}.
}
\description{
Pivots the wide per-candidate columns produced by \code{\link[=fetch_ted_returns]{fetch_ted_returns()}} into
one row per precinct-candidate, and parses the Texas Legislative Council
column encoding \code{"<Candidate><Party>_<YY><Type>_<Office>"}
(for example \code{"BidenD_20G_President"} -> candidate \code{"Biden"}, party \code{"D"},
year \code{2020}, election type \code{"G"}, office \code{"President"}).
}
\details{
The leading three characters of \code{CNTYVTD} are the Texas county FIPS-style
code; the remainder identifies the voting district (precinct) within the
county.
}
\examples{
raw <- tibble::tibble(
  CNTYVTD = c("0010001", "0010002"),
  VTDKEY = 1:2,
  BidenD_20G_President = c(357, 205),
  TrumpR_20G_President = c(791, 1548)
)
tidy_ted_returns(raw)
}
\seealso{
\code{\link[=fetch_ted_returns]{fetch_ted_returns()}}, \code{\link[=build_tables]{build_tables()}}
}
