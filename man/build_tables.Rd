% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{build_tables}
\alias{build_tables}
\title{Build a normalized precinct / candidate / votes schema}
\usage{
build_tables(returns)
}
\arguments{
\item{returns}{Tidy returns from \code{\link[=tidy_ted_returns]{tidy_ted_returns()}}, optionally passed
through \code{\link[=add_cage_keys]{add_cage_keys()}}.}
}
\value{
A named list of three tibbles: \code{precinct}, \code{candidate}, and \code{votes}.
}
\description{
Splits tidy returns into a small relational "database": a \code{precinct} table
(one row per \code{cntyvtd}), a \code{candidate} metadata table (one row per
candidate, carrying any CAGE keys added by \code{\link[=add_cage_keys]{add_cage_keys()}}), and a \code{votes}
table keyed by \code{precinct_id} and \code{candidate_id}.
}
\examples{
returns <- tibble::tibble(
  cntyvtd = c("0010001", "0010001"), county_fips = c("001", "001"),
  candidate = c("Biden", "Trump"), party = c("D", "R"),
  year = 2020L, election_type = "G", office = "President",
  votes = c(357L, 791L)
)
build_tables(returns)
}
\seealso{
\code{\link[=write_returns_parquet]{write_returns_parquet()}}, \code{\link[=open_returns_parquet]{open_returns_parquet()}}
}
