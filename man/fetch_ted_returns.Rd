% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fetch_ted_returns}
\alias{fetch_ted_returns}
\title{Fetch precinct (VTD) election returns from the Texas Capitol Data Portal}
\usage{
fetch_ted_returns(
  election,
  office,
  base_url = "https://ted.capitol.texas.gov/api/Offices",
  max_tries = 3
)
}
\arguments{
\item{election}{Integer election id used by the TED API.}

\item{office}{Integer office id within that election.}

\item{base_url}{API base URL. Defaults to the public TED endpoint.}

\item{max_tries}{Number of times to retry a failed request.}
}
\value{
A \link[tibble:tibble]{tibble} of wide returns with \code{CNTYVTD}, \code{VTDKEY},
and one column per candidate named
\code{"<Candidate><Party>_<YY><Type>_<Office>"} (e.g. \code{"BidenD_20G_President"}).
}
\description{
Downloads precinct-level ("VTD") returns for a single office in a single
election from the Texas Legislative Council TED API, the machine-readable
source behind the Secretary of State returns
(\url{https://data.capitol.texas.gov}). The returned frame is \emph{wide}: one integer
column per candidate. Use \code{\link[=tidy_ted_returns]{tidy_ted_returns()}} to reshape it.
}
\details{
Election and office identifiers come from the Capitol Data Portal CKAN
catalogue. For example, election \code{377} is the 2020 General election and
office \code{1} is President/Vice-President.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# 2020 General (377), President (1)
raw <- fetch_ted_returns(election = 377, office = 1)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tidy_ted_returns]{tidy_ted_returns()}}
}
