% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cage.R
\name{add_cage_keys}
\alias{add_cage_keys}
\title{Attach CAGE-style identifiers to tidy returns}
\usage{
add_cage_keys(returns, state = "TX", crosswalk = tx_president_2020_cage)
}
\arguments{
\item{returns}{Tidy returns from \code{\link[=tidy_ted_returns]{tidy_ted_returns()}}.}

\item{state}{Two-letter state abbreviation. Defaults to \code{"TX"}.}

\item{crosswalk}{A candidate/party -> \code{name_snyder} lookup. Defaults to
\link{tx_president_2020_cage}.}
}
\value{
\code{returns} with added \code{state}, \code{office_cage}, \code{dist}, \code{type}, and
\code{name_snyder} columns.
}
\description{
Adds the constituency-level identifiers used by CAGE, the "Candidates in
American General Elections" dataset (Cha, Kuriwaki, and Snyder,
\url{https://doi.org/10.7910/DVN/DGDRDT}): \code{state}, a CAGE office label
(\code{office_cage}), \code{dist}, \code{type}, and the standardized \code{name_snyder}.
}
\details{
In CAGE, \code{dist} is the congressional-district number for the U.S. House, the
Senate class for the U.S. Senate, and blank for President and statewide
executives; it is left \code{NA} here for president. \code{name_snyder} values are
taken from \code{crosswalk}.
}
\examples{
returns <- tibble::tibble(
  cntyvtd = "0010001", county_fips = "001",
  candidate = "Biden", party = "D", year = 2020L,
  election_type = "G", office = "President", votes = 357L
)
add_cage_keys(returns)
}
\seealso{
\code{\link[=validate_totals]{validate_totals()}}
}
